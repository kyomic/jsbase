{ 
  "name": "main",
  "version": "1.0.0",
  "root": "aaa",   
  "spm": {
    "alias": {
      "jquery": "jquery"
    },
    "output": ["main.js"]
  },
  "spmConfig": {
       "deploy": {
          "to": "{{root}}/{{name}}/{{version}}"
      }
  }
}